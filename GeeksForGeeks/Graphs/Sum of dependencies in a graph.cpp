// Given a directed graph with n nodes. 
// If there is an edge from u to v then u depends on v. O
// ur task is to find out the sum of dependencies for every node.

// Approach: Answer is number of edges

using namespace std;
int main(){
	int t;
	cin>>t;
	while(t--){
	    int n,e;
	    cin>>n>>e;
	    cout<<e<<endl;
	    while(e--){
	        int u,v;
	        cin>>u>>v;
	    }
	}
	return 0;
}